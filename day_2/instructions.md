# Word Frequency Counter

A program that takes a piece of text, splits it into words, and counts how many times each word appears. At the end it prints a sorted summary from most frequent to least.

Store a multi-line string of text in your code (a paragraph or a few sentences)
Split it into individual words
Count each word's frequency using a dictionary
Print the results sorted from most → least frequent
Display the top 5 most common words